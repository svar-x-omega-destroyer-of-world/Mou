[phases.setup]
nixPkgs = ["python313", "tesseract"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && python seed.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
